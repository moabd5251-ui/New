node_modules/
dist/
.DS_Store
*.log

# Local secrets. `.env.example` is the committed template and stays tracked.
.env
.env.local

# Local databases
coupons.db
server/coupons.db

# Generated by test runs — regenerated on every `npx playwright test`, and
# committing them means every run shows up as a diff.
playwright-report/
test-results/
